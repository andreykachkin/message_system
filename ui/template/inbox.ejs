<h1 class="page-header">Входящие</h1>
<div class="table-responsive">
    <table class="table table-striped table-hover" ng-show="MessagesLength !== 0">
        <thead>
        <tr>
            <th></th>
            <th>Отправитель</th>
            <th>Текст сообщения</th>
            <th>Дата</th>
        </tr>
        </thead>
        <tbody>
            <tr ng-repeat="message in messages | orderBy : sortDate" messages-list="inbox"></tr>
        </tbody>
    </table>
    <p class="text-center" ng-show="MessagesLength == 0">У вас нет сообщений</p>
</div>


